'''
Define a function max that takes two number as
 arguments and returns the largest of them.
use the if-else construct.
'''

def max(x, y):
	'''Takes in two arguments and returns
	 the largest of the two.'''
	if x > y:
		return x
	return y
